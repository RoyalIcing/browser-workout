{
  "scripts": {
    "test": "playwright test --browser=all --workers=1 --timeout=0",
    "test:chromium": "playwright test --browser=chromium --workers=1 --timeout=0",
    "test:firefox": "playwright test --browser=firefox --workers=1 --timeout=0",
    "test:webkit": "playwright test --browser=webkit --workers=1 --timeout=0"
  },
  "devDependencies": {
    "@playwright/test": "^1.23.2"
  }
}
